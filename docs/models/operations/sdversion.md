# SdVersion

The model for the training. Can be set to 'FLUX_DEV' for FLUX_DEV specific pricing or can be omitted.

## Example Usage

```python
from leonardo_ai_sdk.models.operations import SdVersion

value = SdVersion.FLUX_DEV
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `FLUX_DEV` | FLUX_DEV   |